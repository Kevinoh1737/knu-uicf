# KNU UICF 교육사업팀 운영 시스템

강원대학교 산학협력단 교육사업팀이 쓰는 내부 시스템. 기업 조사 → 상담 → 교육과정 설계 →
강사 배정·계약 → 수강생 → 만족도까지 한 줄로 잇는다. 모든 사용자 문구는 한국어다.

프로덕션: https://knu-uicf-edu.synthya.ai · 저장소: Kevinoh1737/knu-uicf
(`knu-eduflow.vercel.app` 도 같은 배포를 가리키지만 정식 주소가 아니다 — 공유 카드·알림 링크는 위 주소를 쓴다.)

---

## 0. 이 프로젝트는 moawa 가 아니다

**`/Users/kevinoh/moawa` 는 완전히 다른 사업이고 다른 저장소다.** 읽지도 고치지도 않는다.

이 규칙이 여기 맨 앞에 있는 이유: 2026-08-21까지 이 프로젝트에는 지침 파일이 없어서,
moawa 폴더에서 열린 세션이 **moawa 의 지침(무료 정책·BILLING_MODE·quota 규칙)을 들고**
이 코드를 고쳤다. 그 세션의 상태 파일 5개가 이 저장소에 커밋되기까지 했다.

- **작업은 반드시 이 폴더에서 연 세션에서 한다.** 세션의 작업 폴더가 메모리·지침·임시파일
  위치를 정한다 — 다른 곳에서 열면 남의 프로젝트 기억을 들고 오게 된다.
- moawa 의 코드를 **가져다 쓰지 않는다.** 패턴을 참고할 수는 있으나 옮겨 적고 출처를 남긴다.
- moawa 의 도메인·계정·키를 빌려 쓰지 않는다. 특히 `@moawa.co.kr` 로 강원대 계약서를
  보내면 받는 강사는 피싱으로 의심한다. 기능이 아니라 신뢰의 문제다.

---

## 1. 스택

- **Next.js 16** App Router (React 19) · TypeScript · Vercel
- **Supabase Postgres** + Storage. 접근은 서버에서만(`lib/supabase/admin`), RLS 는 전면 차단
- **Gemini** — 역할별 모델(`lib/ai/models.ts`): fast(추출) / balanced(조사·전사) / deep(분석·설계)
- 인증은 **팀 공용 접속코드 하나**(`TEAM_ACCESS_CODE`). 사용자·부서 구분이 아직 없다 —
  코드를 아는 사람은 전부 본다. 개별 계정 분리는 베타 이후 과제다.
- 스키마는 `supabase/migrations/` 에 파일로 쌓는다. 자동 적용이 아니다(§3).

## 2. 지금 상태 — 베타

2026-08-21부터 **교육사업팀이 직접 쓴다.** 실 데이터가 쌓이는 중이고, 규칙이 달라졌다:
속도보다 **남의 일을 망치지 않는 것**이 위다.

- 실 사용자가 화면을 열어 둔 채로 배포가 나가면 그 사람의 작업이 끊긴다.
- 스테이징 데이터베이스가 없다. 로컬 개발도 **프로덕션 DB 를 본다**(§3).
- 무엇이 실패했는지는 `/admin` 에서 본다(§5).

---

## 3. 데이터베이스

`.env.local` 의 `POSTGRES_URL_NON_POOLING` 으로 직접 붙는다. **다만 그 주소 그대로는 안 된다.**

- `db.<ref>.supabase.co` 는 **IPv6 주소만** 있다. IPv6 로 못 나가는 망에서는 이름조차 안 풀린다.
- 풀러로 우회한다 — 호스트 `aws-0-ap-northeast-2.pooler.supabase.com:5432`,
  사용자 `postgres.<ref>`, 비밀번호는 같다. 세션 모드라 DDL 이 된다.
- 서비스 롤 키(PostgREST)로는 **데이터만** 읽고 쓴다. 스키마는 못 바꾼다.

### 규칙

- **조회는 자유롭게 한다.** 확인하지 않고 말하는 것보다 낫다.
- **스키마 변경은 실행할 SQL 을 먼저 보여 주고 승인받는다.** 스테이징이 없어서 실수가
  곧 실 데이터 사고다.
- **추가·완화만 한다(additive-only).** drop·rename·타입 축소·NOT NULL 추가는 두 단계로 쪼갠다.
- 적용한 뒤 **결과를 직접 확인한다.** 스크립트가 찍은 성공/실패 문구를 믿지 않는다 —
  실제로 성공한 마이그레이션을 "실패"로 찍은 적이 있다. DB 에 다시 물어봐서 확인한다.
- 시험 행을 넣어 새 경로가 실제로 되는지 보고, **넣은 것은 지운다.**

---

## 4. 배포

**자동 배포가 아니다.** `npx vercel --prod` 로 올리고 별칭을 손으로 붙인다.

```bash
npx vercel --prod --yes
npx vercel alias set <새-배포-주소> knu-uicf-edu.synthya.ai
npx vercel alias set <새-배포-주소> knu-eduflow.vercel.app   # 옛 주소도 함께 옮긴다
```

### 배포 전에 반드시 하는 것

1. **영향 분석.** `npm run pre-deploy` 를 돌려 지금 올라가 있는 것과 무엇이 달라지는지 본다 —
   바뀐 화면·라우트, 아직 적용 안 된 마이그레이션, 사용자에게 보이는 변화.
   결과를 대표에게 **말로 정리해서** 보고한다. 파일 목록이 아니라 "무엇이 달라지는가"다.
2. **스키마가 먼저.** 마이그레이션을 적용한 다음에 코드를 올린다. 반대면 새 코드가 없는 칸을 찾는다.
3. **작업트리를 비운다.** `vercel` 은 git 이 아니라 **로컬 폴더를 통째로 올린다** —
   커밋 안 한 파일도 같이 간다. `.env*` 는 기본 제외되지만 나머지는 아니다.
4. **시간을 고른다.** `/admin` 의 '언제 쓰나' 에서 한산한 시간대를 본다. 사람이 쓰는 중에
   배포하면 그 사람의 화면이 새로 뜬다. 기록이 아직 적으면 이른 아침이나 업무 후로 잡는다.
5. **몰아서 한다.** 베타 중에는 고칠 때마다 올리지 않는다. 묶어 두었다가 한 번에 올린다 —
   배포 한 번이 곧 사용자 한 번의 방해다.
6. **직전에 알린다.** "지금 배포합니다. 바뀌는 것은 A·B·C 입니다" 라고 말한 뒤 실행한다.

### 배포 후

- `deploy-log.md` 에 한 줄 남긴다. **다음 영향 분석의 기준점이 이 줄이다.**
- 별칭이 새 배포를 가리키는지, 로그인 화면이 뜨는지 확인한다.
- 응답 헤더의 `x-vercel-id` 가 `icn1` 인지 본다 — DB 와 같은 서울 지역이어야 한다.

---

## 5. 베타 관찰

`/admin` (메뉴에 없다. 주소로 연다. 교육사업팀이 보는 화면이 아니다.)

- 화면의 `fetch` 를 한 자리에서 감싸 **모든 API 호출과 실패**를 남긴다(`app/telemetry-client.tsx`).
  서버 로그로는 부족하다 — 아이폰 `.m4a` 업로드 실패는 **서버에 닿지도 않아** 어느 로그에도
  없었고, 대표가 오류 문구를 옮겨 주기 전까지 아무도 몰랐다.
- **품질 신호**를 따로 남긴다(`trackQuality`). 실패는 스스로 드러나지만 '반쯤 된 결과' 는
  그렇지 않다 — 메모를 절반만 읽었거나, 결과지 짝짓기를 사람이 여러 번 고쳤거나.
- **기록이 일을 망치지 않는다.** `track()` 은 어떤 오류도 부르는 쪽에 넘기지 않는다.
  개인정보·문서 본문은 담지 않는다. 경로·상태·소요시간·우리가 쓴 안내 문구까지다.

---

## 6. 일하는 방식

- **측정해서 말한다.** "고쳤습니다" 대신 "재 봤더니 이렇습니다". 화면은 실제 CSS 로 재현해
  띄워 보고, 파서는 실제 형식으로 돌려 보고, DB 는 직접 물어본다.
- **컨테이너 선택자 특이성 함정.** `.consulting .upload-zone button` 같은 규칙이 그 안의 새
  버튼을 전부 잡는다. 이 프로젝트에서 다섯 번 겪었다. 새 버튼을 넣을 때는 계산된 스타일을
  브라우저에서 확인하고, 순서에 기대지 말고 선택자를 좁힌다.
- **격자는 `minmax(0,1fr)`.** 그냥 `1fr` 은 최소폭이 '안 줄어드는 내용의 폭' 이라 긴 이름이
  옆 칸을 밀어낸다.
- **좁은 화면에서 전역 규칙이 버튼을 44px 로 키운다.** 칸도 그만큼 잡아야 삐져나오지 않는다.
- 주석은 **왜** 를 적는다. 무엇을 하는지는 코드가 말한다.
- 사용자 문구에 이모지를 쓰지 않는다. `·` `~` `→` 는 기능적 요소라 괜찮다.

## 7. 명령

`npm run dev` · `npm run build` · `npm run lint` · `npm run pre-deploy`

검증 스크립트는 esbuild 로 묶어 돌린다(각 파일 머리말에 실행법이 있다):
`scripts/check-survey-import.ts` · `scripts/check-telemetry.ts`
