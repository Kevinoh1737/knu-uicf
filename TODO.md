# TODO — 핵심 기능 구현 이후로 미루는 항목

핵심 기능(녹취 STT → 교육과정 설계 → 강사 배정)이 어느 정도 완성된 뒤에 처리한다.
지금 손대면 아직 만들지 않은 기능 위에 다듬기를 하는 셈이라 우선순위를 낮춘다.

## 1. 공시·채용 조회가 잘못된 회사명을 쓴다 (정확도)

`app/api/companies/research/route.ts`가 OpenDART와 채용 검색에 넘기는 이름은 홈페이지에서
긁은 `inferredName`이다. 이 이름은 법인명이 아니라 그룹 브랜드나 슬로건인 경우가 많다.

측정 결과 (더존비즈온, 2026-08-15):

- 홈페이지 `<title>` = `더존ICT그룹` → DART "일치하는 기업 없음", 채용 0건
- 같은 함수에 `더존비즈온`을 직접 넘기면 → DART 확인(대표 이강수·지용구, 2025년 매출 4,463억),
  IT 채용 5건

즉 두 데이터 소스는 정상이고 이름만 틀렸다. 화면에는 "공시자료 없음"이 사실처럼 표시되는데,
비전문가 담당자에게는 오류 메시지보다 이쪽이 더 위험하다.

고칠 방향: Gemini가 본문을 읽고 확정한 법인명(`report.companyName`)으로 조회를 다시 하거나,
조회 전에 법인명을 먼저 확정하는 단계를 둔다. 지금은 조회가 Gemini 호출보다 먼저라 확정된
이름을 쓸 수 없는 순서 문제다.

## 2. 홈페이지 TLS 실패가 불친절하게 끝난다 (오류 문구)

인증서 검증에 실패하는 기업 홈페이지가 국내에 드물지 않다. 확인된 사례: 휴젤(`hugel.co.kr`),
강원랜드(`kangwonland.co.kr`). 현재는 `fetch failed`만 표시돼 담당자가 원인을 알 수 없다.
"홈페이지 보안인증서 문제로 접속할 수 없음. 다른 주소나 회사소개 PDF로 시도" 수준의 안내가 필요하다.

## 3. Turbopack dev 가 CSS 를 갱신하지 않을 때 (환경) — 원인 규명됨

> **2026-08-15: 3번과 4번은 같은 원인이었다.** CSS 가 안 바뀌던 날 `.next` 안에
> `dev 2`, `dev 3`, `server/app 3`, `static/chunks 3` 이 함께 있었다. iCloud 가 `.next` 를
> 동기화하면서 만든 충돌 사본이 Turbopack 워처를 망가뜨린다. 증상은 "중간부터 갱신이 멈춤"으로,
> 초반 수정은 반영되고 이후 수정만 조용히 무시된다 — 가장 헷갈리는 형태다.
>
> 처방: `rm -rf .next` 후 재시작. `.next/dev` 만 지우는 것으로는 부족했다.
> 근본 해결은 4번(저장소를 iCloud 밖으로)이다.



`app/globals.css` 에 규칙을 덧붙였는데 화면에 반영되지 않으면, 파일이 아니라 dev 서버의 CSS
청크가 고착된 것이다. 이번 작업에서 두 번 발생했다. 확인·해결:

```
curl -s "http://localhost:3000/$(curl -s http://localhost:3000/login | grep -o '_next/static/chunks/app_globals[^"]*\.css' | head -1)" | grep -c '새규칙이름'
```

0이 나오면 `rm -rf .next/dev` 후 dev 서버 재시작.

## 4. iCloud 동기화가 만드는 중복 파일 (환경) — 완화됨

> **2026-08-15 처방:** `.next` 를 `.next.nosync` 심볼릭 링크로 바꿨다. iCloud 는 `.nosync` 로
> 끝나는 항목을 동기화하지 않으므로 빌드 산출물이 더 이상 충돌 사본을 만들지 않는다.
> `npm run lint` 의 무시 경로에 `.next.nosync` 를 추가했다 (안 하면 빌드 산출물까지 훑어
> 오류가 8천 건으로 늘어난다).
>
> 소스 파일(`README 2.md` 등)의 중복은 그대로다. 근본 해결은 아래대로 저장소를 옮기는 것이다.



프로젝트가 `~/Documents` 아래에 있어 iCloud가 충돌 시 `" 2"` 사본을 만든다.
`README 2.md`, `package 2.json`, `public/favicon 2.svg`의 정체가 이것이다.
`.next/types/routes.d 2.ts`가 생기면 `npx tsc --noEmit`이 중복 선언 오류로 깨진다(한 번 발생, 삭제함).

근본 해결은 저장소를 iCloud 동기화 밖으로 옮기는 것이다. 그 전까지는 타입체크가 갑자기
깨지면 `find . -name "* 2.*" -not -path "./node_modules/*"` 를 먼저 확인한다.

## 5. 90분 넘는 녹취 — 분할 전사 (기능)

현재 한 번에 올릴 수 있는 녹취는 90분까지다. 근거는 실측이다 — 58분 녹취의 전사가 API 시간
221초(분당 3.8초), 출력 18,855토큰으로 **잘림 없이 완주**했다. Vercel Pro의 800초 한도(예산 780초)
안에서 90분은 약 340초, 부대 작업 포함 470초 수준이라 여유가 있다.

90분을 넘기려면 걸리는 것은 라우트 시간이 아니라 **브라우저 메모리**다. 변환 시 오디오 1시간당
약 440MB를 잡으므로 2~3시간짜리는 탭이 위험해진다. 이때는 10~15분 단위로 쪼개 병렬 전사하는
방식이 필요하다. 브라우저가 이미 디코딩된 샘플을 들고 있으므로 조각내어 여러 파일로 내보내는
비용은 거의 없다.

인수인계 문서가 품질 이유로도 같은 방향을 지목한다 — 긴 파일에서 전사가 요약·축약되는 경향.
다만 이번 58분 실측에서는 축약이 관찰되지 않았으니, 품질 근거는 다시 확인이 필요하다.

## 6. 실패한 상담 기록은 재시도할 수 없다 (기능)

> 삭제 기능은 2026-08-15 추가됨. 실패한 기록을 지우고 다시 올리는 것은 이제 가능하다.
> 남은 것은 "이미 올라간 파일로 재처리" 경로다.

`company_consultations.storage_path` 에 UNIQUE 제약이 걸려 있어, 이미 올라간 파일로 처리를
다시 요청하면 전사에 도달하기 전에 INSERT가 막힌다(실측: 0.98초 만에 422).

2026-08-15 실제로 발생했다. Gemini 503으로 전사가 실패해 행이 `failed` 로 남았는데, 같은
스토리지 파일로 재시도가 불가능했다. 담당자에게 남은 방법은 같은 파일을 다시 업로드하는 것뿐이고
(새 업로드는 새 uuid 경로라 통과한다), 실패한 행은 화면에 계속 남는다. 삭제 UI도 없다.

고칠 방향: `status = 'failed'` 인 기존 행이 있으면 새로 INSERT 하지 말고 그 행을 재사용해
재처리한다. 상담 기록 삭제도 함께 필요하다.

관련해서 파일 입력이 조용히 무시되던 버그는 별건으로 고쳤다(같은 파일 재선택 시 `change`
이벤트가 발생하지 않아 아무 반응이 없던 문제). 실패 후 재시도가 막히는 위 제약과 겹쳐서
"눌러도 아무 일이 없다"로 보였다.

## 7. 사이드바 운영자 이름이 목데이터다 (정리)

접근 코드 방식에는 개인 식별이 없는데 사이드바는 `김서윤 · 관리자`를 보여준다.
`app/page.tsx`에서 기업 생성 시 `owner`에도 같은 이름이 들어간다. 표기 방식은 제품 판단이 필요하다.

## 8. 미사용 화면 컴포넌트 정리 (정리)

`app/page.tsx`에 `CoursesTab`, `StudentsTab`, `Surveys`가 목데이터와 함께 남아 있으나
네비게이션에 연결되어 있지 않다. 교육과정 설계를 실제로 만들 때 이 스케치를 되살릴지 버릴지 정한다.

> 2026-08-15: `Instructors` 스케치와 목데이터는 제거했다. 실제 강사 풀 화면
> (`app/instructors-panel.tsx`)이 그 자리를 대신한다. 남은 lint 오류는 10건.

`tests/rendered-html.test.mjs`는 존재하지 않는 `dist/server/index.js`를 테스트하는 Codex 템플릿
잔재라 실행하면 실패한다.

## 9. 기업 목록 검색창이 작동하지 않는다 (기능)

`app/page.tsx` 기업 목록의 검색 입력에 `value` 도 `onChange` 도 없다. 글자는 쳐지지만 목록은
걸러지지 않는다. 기업이 세 곳뿐인 지금은 불편이 없어 미룬다.

기업이 20곳을 넘어가면 필요해진다. 그때는 이름·산업에 더해 **단계로 거르는 것**(예: 강사 미배정만)
이 실제로 쓰일 가능성이 높다. 카드가 이미 다음 할 일·정체 일수를 보여주므로, 검색보다 단계 필터가
먼저일 수도 있다.

## 10. 계정 하나를 여러 명이 함께 쓸 때 (동시 작업)

2026-08-16 코드 점검. 현재 인증은 팀 공용 접근 코드 하나이고, 팀원 여러 명이 동시에 쓰는 것을
전제로 만들어져 있지 않다. **막히는 사고보다 조용히 없어지는 사고가 위험하다** — 충돌 시
대부분 나중에 저장한 쪽이 이기고 앞사람 작업이 경고 없이 사라진다.

이미 안전한 것부터: 계약번호는 DB 시퀀스(`next_contract_no`)라 겹치지 않는다. 수강 연결
(`session_learners`)은 유니크 제약 + upsert라 중복이 안 생긴다. 진행 중인 계약이 있으면 강사
교체를, 서명된 계약이 있으면 교육과정 삭제를 막는다. 되돌릴 수 없는 것들은 이미 잠겨 있다.

### 9-1. 기업 재조사가 질문지를 지운다 (데이터 손실 · 급함)

`app/api/companies/route.ts` 의 POST 는 `website_url` 이 같으면 덮어쓰기(upsert)인데 몸체에
`questions: body.questions || []` 가 들어 있다. 화면이 보내는 draft(`app/page.tsx`, 조사 저장)에는
질문지가 없으므로, **이미 조사된 회사를 다시 조사하면 다듬어 둔 니즈 질문지가 빈 배열이 된다.**
`research`·`intelligence`·`crawl` 도 함께 덮인다. (`stage`·`contact` 는 몸체에 없어 보존된다.)

혼자 써도 나는 사고지만, 여러 명이면 "그 회사 이미 조사된 걸 몰랐다"가 일상이라 사실상 상시
위험이다. 경고도 복구 경로도 없다.

고칠 방향: 기존 행이 있으면 `questions` 는 건드리지 않는다(전달된 경우에만 갱신). 조사 결과
덮어쓰기 자체도 "다시 조사하면 기존 결과를 대체합니다" 확인을 받는 편이 안전하다.

### 9-2. 명단 엑셀 동시 업로드 (중복 · 급함)

`app/api/learners/route.ts` 의 중복 판정은 읽고 → 판단하고 → 넣는 방식이라 동시 요청에 취약하다.

- 이메일 있는 사람: 유니크 인덱스(`learners_company_email_idx`)가 막아 한쪽이 **422** 로 튕긴다.
  데이터는 안전하지만 원인을 알 수 없는 실패 메시지가 뜬다.
- 이메일 없는 사람: 유니크 인덱스가 없어 **그대로 두 번 들어간다.** 고객사 명단은 이메일이
  빠져 오는 일이 많아 이쪽이 더 현실적이다.

고칠 방향: 삽입을 upsert 로 바꾸고 유니크 충돌 시 재조회 후 갱신으로 되돌아간다.

### 9-3. 마지막 저장이 이긴다 (덮어쓰기)

질문지(`companies/[id]/questions`), 담당자(`companies/[id]/contact`), 진행 상태
(`companies/[id]/stage`), 교육과정 수정 모두 버전 확인 없이 통째로 덮어쓴다. A가 질문을
다듬는 동안 B가 열어 둔 화면에서 저장하면 A의 수정은 사라지고 **양쪽 다 자기 것이 저장됐다고
믿는다.**

고칠 방향: `updated_at` 을 함께 보내 서버에서 비교하고, 다르면 409 + "다른 사람이 먼저
저장했습니다. 새로 불러온 뒤 다시 저장해 주세요."

### 9-4. 화면이 스스로 갱신되지 않는다 (9-3의 뿌리)

기업 목록은 화면 진입 시 한 번만 불러온다(`app/page.tsx` 의 `useEffect(..., [])`). 탭을 켜 둔
채 자리를 비우면 몇 시간 전 데이터를 보게 되고, 그 상태의 저장이 9-3을 만든다. 그 사이 다른
사람이 지운 회사를 누르면 404 가 난다.

고칠 방향: 창이 다시 활성화될 때(`visibilitychange`) 목록 재조회. 실시간 동기화까지는 필요 없다.

### 9-5. 강사 배정 경합 · 중복 예약

- 두 사람이 서로 다른 강사를 배정하면 나중 것이 이긴다. 초안 계약서도 나중 강사로 따라간다 —
  먼저 배정한 사람의 화면에는 여전히 원래 강사가 보인다.
- **같은 강사를 같은 날·같은 시간 두 교육에 배정해도 아무 검사가 없다.** 이건 동시 작업과
  무관하게 지금 열려 있는 구멍이다. 배정 시 해당 강사의 일정 충돌을 확인해야 한다.

### 9-6. 한 강의에 계약서 두 장

`app/api/contracts/route.ts` 의 "이미 계약서 있음" 검사도 읽고-넣기다. 동시 요청이면 둘 다
통과한다. DB 제약이 `contracts_session_unique (course_session_id, contract_no)` 라 번호가 다르면
막히지 않는다 — 한 강의에 계약서 두 장, 번호 두 개 소모.

고칠 방향: `course_session_id` 위에 살아 있는 상태만 대상으로 하는 부분 유니크 인덱스를 건다.

### 9-7. 누가 했는지 남지 않는다 (감사)

계약 이력의 행위자가 `"교육사업팀"` 문자열로 고정돼 있다(`app/api/contracts/route.ts`).
**계약은 감사 대상인데 누가 만들고 누가 철회했는지 기록이 없다.** 기업·교육과정 삭제도 마찬가지다.

접근 코드가 하나라 **한 사람만 접근을 회수할 수 없고**, 코드를 바꿔도 이미 발급된 세션은
12시간 만료(`SESSION_MAX_AGE_SECONDS`)까지 살아 있다.

고칠 방향(가벼운 순): 로그인 시 이름을 고르게 해 세션 토큰에 담고 `actor` 에 기록 → 필요해지면
사람별 계정으로 승격. 산학협력단 계정 정책 확인이 먼저다.

### 우선순위

| 순위 | 항목 | 규모 |
|---|---|---|
| 급함 | 9-1 질문지 보존 | 한 줄 수준 |
| 급함 | 9-2 명단 upsert + 충돌 재시도 | 30분 |
| 권장 | 9-3 `updated_at` 낙관적 잠금 | 1~2시간 |
| 권장 | 9-4 창 복귀 시 재조회 | 30분 |
| 나중 | 9-5 강사 일정 충돌 검사 | 반나절 |
| 나중 | 9-6 계약서 부분 유니크 인덱스 | 30분 |
| 나중 | 9-7 행위자 기록 | 반나절 |

## 10. 결과 PDF에 실명이 들어간다 — 관리자 판단 대기

`/api/surveys/[id]/report` 는 요약 아래에 **응답 상세**를 싣는다. 사람마다 한 줄로 문항별
점수, 그 아래에 적어 준 글을 이름·부서와 함께 옮긴다(2026-08-19, 대표 요청).

지금은 이것이 맞다 — 수강생에게 익명을 약속하는 문구가 없고(질문지의 안내 문구가 비어
있다), 발송 기록에 누가 답했는지 이미 남는다.

**다만 이 PDF 를 고객사에 그대로 넘기면 "진도가 빨랐다"고 쓴 직원이 회사에 특정된다.**
교육사업팀 관리자 의견을 듣고 정한다. 고를 수 있는 길:

1. 지금처럼 전부 싣는다(내부 검토용으로만 쓴다는 전제).
2. 이름을 빼고 `응답 1·2·3` 으로 싣는다 — `lib/survey-report.ts` 의 이름 자리만 바꾸면 된다.
3. 내부용·고객사용 두 벌로 나눈다 — 라우트에 질의 인자 하나(`?anonymous=1`).

2·3 을 고르면 **질문지 안내 문구에 익명 여부를 적는 일**이 함께 따라와야 한다. 지금은
아무 말도 하지 않는 상태라, 어느 쪽으로 정하든 수강생이 그 사실을 알고 답해야 한다.

## 11. 동명 법인이면 매출을 못 찾는다 — 한주케미칼에서 실측 (시연 후 처리)

2026-08-20 확인. 한주케미칼 조사에서 매출이 비어 있던 원인은 "비상장이라 공시 없음"이
아니라 **DART 에서 다른 법인을 골랐기 때문**이다. 두 건 다 실제로 확인했다.

| | 앱이 고른 쪽 | 실제 그 회사로 보이는 쪽 |
|---|---|---|
| corp_code | `00163451` | `01502912` |
| 대표 | 최승환 | 김동준 |
| 설립 | 1986-02-03 | 2011-04-22 |
| 주소 | 화성시 마도면 | 화성시 남양읍 |
| 사업자번호 | 131-81-03805 | 124-87-13030 |
| 공시 | 감사보고서 7건, **2003~2009 가 마지막** | 감사보고서 6건, **2021~2026 매년** |

홈페이지는 둘 다 `hanjufire.com` 이라 이것으로는 가릴 수 없다. 실제 숫자(감사보고서
2025.12, 제15기): 매출 **314.4억**(전기 411.3억), 매출원가 189.3억, 자산 269.2억.

### 원인 1 — 같은 이름일 때 고르는 기준이 없다

`lib/company-intelligence/dart.ts` `pickCorp()`:

```
const exact = rows.filter(row => normalizeName(row.corpName) === target);
if (exact.length) return exact.find(row => row.stockCode?.trim()) || exact[0];
```

상장사를 먼저 보고, **둘 다 비상장이면 `exact[0]`** — corpCode.xml 에 먼저 나온 것이다.
즉 파일 순서가 결정한다. 비상장 동명 법인에서는 언제든 다시 난다.

고칠 방향: 이름이 같은 후보가 둘 이상일 때만 `list.json` 을 불러 **최근에 공시한 쪽**을
고른다(휴면 법인은 공시가 끊겨 있다). 후보가 하나면 지금처럼 그냥 쓴다 — 호출이 늘지 않는다.

### 원인 2 — 감사보고서 표에서 주석 번호를 금액으로 읽는다

같은 파일 `amountFor()` 의 패턴은 라벨 뒤 `(주1)` 꼴만 건너뛴다:

```
`${spacedLabel(label)}\s*(?:\(주[^)]*\))?\s*(${NUMBER})`   // NUMBER = \(?-?[\d,]{4,}\)?
```

그런데 이 보고서의 손익계산서는 주석 칸이 괄호 없이 `16,19` 로 적혀 있다:

```
Ⅰ. 매출액   16,19   31,436,330,218   41,134,114,246
```

`16,19` 가 `[\d,]{4,}` 에 걸려 **매출액 = 1,619 원**이 된다. 대차 검산은 재무상태표만
보므로 이 값을 걸러 내지 못한다. 법인을 바로잡아도 이게 남아 있으면 매출이 틀리게 나온다.
`00163451` 의 2008년 보고서는 주석 칸이 없어 정상 파싱된다 — 문서마다 다르다.

고칠 방향: 라벨 뒤 숫자를 하나만 보지 말고 후보를 여러 개 받아, **자릿수가 크고 같은 행의
다른 기수(전기) 값과 크기가 맞는 것**을 고른다. 또는 주석 칸(짧은 정수 나열)을 먼저 버린다.

### 문구도 사실과 다르다

지금은 `공시 의무가 없어 재무 수치 없음 (사업보고서·감사보고서 모두 없음)` 이라고 적는데,
`00163451` 에도 감사보고서는 있다 — **최근 3년치가 없을 뿐이다**(`auditFinancials` 가
`year-3` 부터 본다). "최근 N년 공시 없음" 으로 바꾸는 편이 정확하다.

### 왜 지금 고치지 않았나

2026-08-20 시연 당일이라 대표 판단으로 미뤘다. 조사 경로 전체를 다시 검증해야 하는데
시연에서 라이브로 새 기업을 조사할 예정이었다.

## 12. 현장은 전화번호로 돌아가는데 시스템은 이메일만 안다 (2026-08-20 시연)

시연에서 나온 이야기: **수강생에게서 받는 것은 전화번호다.** 수업이 끝나자마자 그 자리에서
휴대폰으로 만족도 링크를 보내기 때문이다. 이메일은 현장에서 받지 않는다.

코드를 확인해 보니 시스템은 정확히 반대로 되어 있다 (2026-08-21 실측):

| | 지금 |
|---|---|
| `learners` 표 | `email` 은 있고 **`phone` 칸이 없다** |
| 명단 판독 (`app/api/learners/extract/route.ts`) | 휴대전화번호를 **일부러 버린다** — 제외 목록에 명시 |
| 만족도 발송 (`app/api/surveys/[id]/send/route.ts`) | 이메일로만 나간다. 주소 없는 수강생은 건너뛴다 |

그래서 구글폼 결과지 들여오기가 필요했던 것이다 — 발송 경로가 현장과 맞지 않으니 밖에서
받아 온 결과를 들여올 수밖에 없었다. 들여오기는 증상에 대한 대처였고 원인은 여기 있다.

**이메일은 일단 그대로 둔다(대표 판단).** 지우지 않는다.

정하고 나서 손대야 할 것 — 아직 정하지 않았다:

- 전화번호를 **받을 것인가**. 받는다면 `learners` 에 칸을 더하고, 판독 프롬프트에서 버리던
  것을 받도록 바꾸고, 명단 파일에 실려 오는 생년월일·사번은 계속 버려야 한다.
- 받는다면 **무엇으로 보낼 것인가**. 문자·알림톡은 발신번호 등록과 비용이 따라온다.
  지금처럼 사람이 링크를 손으로 보내는 것도 방법이고, 그러면 시스템은 번호만 보관한다.
- **보관 자체가 늘어나는 책임이다.** 지금 이 시스템은 접속코드 하나를 아는 사람이 전부 본다
  (§ AGENTS.md 1). 수강생 전화번호가 들어오면 그 명부가 통째로 그 뒤에 놓인다. 개별 계정
  분리보다 먼저 들어오면 안 되는 데이터인지 함께 판단한다.

## 13. 수업이 끝난 뒤의 최종보고서 (2026-08-20 시연) — 샘플 대기

모든 수업이 끝나면 **HRD Net 같은 외부처와 내부 보고용으로 최종보고서를 만들어 올린다.**
지금 이 시스템에는 그 단계가 없다 — 만족도 결과 PDF 에서 끊긴다.

**샘플을 요청해 두었다. 받기 전에는 구현을 정하지 않는다.** 무엇을 담아야 하는지, 형식이
정해져 있는지, 제출처가 파일을 어떻게 받는지를 모르는 채로 만들면 다시 만들게 된다.

샘플을 받으면 볼 것:

- 그 문서의 칸 중 **이미 우리 DB 에 있는 것이 몇 %인가** (교육과정·수강생·만족도·강사·계약).
  높으면 자동 생성이 값을 하고, 낮으면 사람이 채우는 서식만 뽑아 주는 편이 낫다.
- 제출처가 요구하는 **형식**(hwp·xlsx·pdf). 지금 만들 수 있는 것은 PDF 와 xlsx 다.
  hwp 면 그건 우리가 만들 수 있는 물건이 아니다 — 서식 채우기까지만 돕는다.
- **수업 단위인가 사업 단위인가.** 여러 교육과정을 묶어 한 장으로 내는 것이면 지금 화면
  구조(회사 → 교육과정)와 축이 다르다.

## 14. 강사 카테고리와 자동 배정 추천 (2026-08-20 시연)

강사마다 **성격을 붙여 두고**, 나중에 교육이 많아지면 회사 조사 결과와 맞춰 **가장 적합한
강사를 추천**해 주면 좋겠다는 요청.

카테고리는 관리자가 **수업을 참관하면서 파악한 특징**을 이름으로 만든다. 미리 정해진 목록이
아니라 **계속 새로 추가할 수 있어야 한다.** 강사 한 명에게 **하나 이상** 붙는다(해시태그처럼).

시연에서 나온 예 — 아직 실제 이름은 정해지지 않았다:

- 재밌게 가르치는 강사가 맞을 수업
- 이미 AI 기본 지식이 있는 그룹이라 깊게 들어갈 수 있는 강사가 맞을 수업
- 대표이사들만 모인 수업에 맞을 강사

추천할 때 **이유까지 설명**해야 한다. 이름만 나오면 수업 관리자가 그 판단을 검토할 수 없고,
검토할 수 없는 추천은 결국 안 쓰게 된다.

### 지금 있는 것과의 차이

`instructors` 에 이미 `expertise`·`preferred_style`·`teaching_history` 가 있다. 다만 전부
**자유 서술 글**이라 골라내기·세기·비교가 안 된다. 카테고리는 그것과 다른 물건이다 —
같은 이름을 여러 강사가 공유해야 "이 성격의 강사 목록"이 나온다. 표를 따로 두어야 한다
(카테고리 표 + 강사·카테고리 연결 표).

### 언제 만들 값을 하는가

**지금은 강사가 3명이다.** 3명 중에 고르는 일에 추천이 필요하지 않고, 참관 기록도 아직
쌓이지 않았다. 강사가 늘고 참관으로 붙인 카테고리가 실제로 쌓인 뒤에 추천을 붙인다.

순서를 그렇게 두는 것이 낫다:

1. 카테고리를 **붙일 수 있게만** 먼저 만든다(표 + 강사 화면에서 달기·떼기·새로 만들기).
   이건 지금 만들어도 낭비가 아니다 — 참관 기록이 쌓이기 시작해야 나중에 추천이 근거를 갖는다.
2. 회사 조사 결과와 맞추는 추천은 **카테고리가 실제로 쌓인 뒤에.** 그때 무엇을 근거로
   맞출지(업종·직급 구성·AI 숙련도)를 실제 기록을 보고 정한다.

## 15. 맥·아이폰에서 올린 파일은 이름이 자모 분해된 채로 저장된다 (2026-08-20 시연에서 발견)

시연 기록을 되짚다 찾았다. 어제 16:21 에 올린 녹취의 파일명이 **NFD(자모 분해)** 로 저장돼
있었다. 화면에는 똑같이 보여서 시연 중에 아무도 몰랐다.

실측 (2026-08-21):

| 파일 | 저장 형태 | 글자수 |
|---|---|---|
| `면담녹취_글로벌이엔피_20260521.mp3` (스크립트가 넣음) | NFC | 24 |
| `면담녹취_한주케미칼_20260814.m4a` (시연 중 실제 업로드) | **NFD** | 37 (NFC 로는 23) |

검색이 실제로 깨진다:

```
file_name like '%한주케미칼%'                   → 0건
normalize(file_name, NFC) like '%한주케미칼%'   → 1건
```

### 한 파일이 아니다

| 자리 | 깨짐 / 전체 |
|---|---|
| `instructor_documents.file_name` | **3 / 3** (08-16·08-19·08-20) |
| `company_consultations.file_name` | 1 / 2 |
| 사람이 타이핑한 값(강사명·회사명·수강생명·교육 제목·응답자명) | **0 / 153** |

규칙이 정확하다 — **맥·아이폰에서 올린 파일 이름은 전부 깨지고, 사람이 친 글자는 전부
멀쩡하다.** macOS 파일 시스템이 한글 파일명을 자모 분해해 두기 때문이고, 브라우저가
`File.name` 을 그대로 넘긴다. 우리 코드는 **어디에서도 정규화하지 않는다**(grep 확인).
08-16 부터 계속 이랬으므로 시연이 만든 문제가 아니라 시연이 **드러낸** 문제다.

### 지금 당장 무엇이 깨지나 — 솔직히 말하면 아직 별로 없다

- **파일명으로 검색하는 화면이 아직 없다.** 그래서 오늘의 피해는 거의 없다. 잠재 결함이다.
- 화면 표시는 멀쩡하다. NFC 와 NFD 는 같은 모양으로 그려진다.
- `storage_path` 는 UUID 라 무사하고, 확장자는 ASCII 라 `.m4a`·`.pdf` 판별도 정상이다.
- 하나 걸리는 곳: `app/api/companies/[id]/consultation-briefing/route.ts:128` 이 파일명을
  **AI 프롬프트에 그대로 넣는다**. 모델이 분해된 자모를 보게 된다. 치명적이진 않다.

**터지는 시점은 파일명 검색이나 중복 검사를 붙일 때다.** 그때 "분명히 올렸는데 안 나온다"로
나타나고, 원인이 눈에 안 보여서 찾는 데 오래 걸린다. 지금 고치는 게 싸다.

### 고친 방법 (2026-08-21)

`lib/file-names.ts` 의 `cleanFileName()` 하나로 모았다. 자리마다 `normalize` 를 흩뿌리면
다음에 업로드 경로가 하나 늘 때 거기만 빠지고, 그 사실은 몇 달 뒤에야 드러난다.

파일명을 **저장하는** 자리 전부에 물렸다(7개 파일 8군데):

- `app/api/uploads/consultation-audio` · `consultation-note` · `instructor-document`
- `app/api/companies/[id]/consultations/route.ts` · `.../consultations/note/route.ts` (2군데)
- `app/api/course-sessions/[id]/documents/route.ts`
- `app/api/instructors/route.ts`

**일부러 손대지 않은 곳 둘** — 파일명을 저장하지 않고 확장자만 보는 자리다. ASCII 라
자모 분해의 영향이 없고, 여기에 넣으면 죽은 코드가 된다:

- `app/api/uploads/company-pdf/route.ts` (`.pdf` 인지만 본다, 경로는 UUID)
- `app/api/surveys/[id]/import/route.ts` (`.csv` 인지만 본다)

검증은 `scripts/check-file-names.ts` 9가지. '보기에 같다'가 아니라 **글자로 같은가**를 본다.

이미 들어간 행은 한 번 훑어 고친다 — 추가·완화만 하는 변경이라 안전하다:

```sql
update public.instructor_documents  set file_name = normalize(file_name, NFC)
  where file_name <> normalize(file_name, NFC);
update public.company_consultations set file_name = normalize(file_name, NFC)
  where file_name <> normalize(file_name, NFC);
```

시연 데이터를 지울 때 상담 녹취는 함께 사라지지만 **강사 서류 3건은 강사를 남기면 남는다.**
그때 이 update 를 같이 돌린다.
